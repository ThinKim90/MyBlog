# Google Analytics 4 설정
GA4_PROPERTY_ID=your_ga4_property_id_here
GA4_CREDENTIALS={"type":"service_account","project_id":"your_project_id",...}

# GoatCounter 설정
GC_SITE=your-site.goatcounter.com
GC_API_TOKEN=your_goatcounter_api_token_here

# 사용법:
# 1. Google Cloud Console에서 서비스 계정 생성
# 2. GA4 속성에 서비스 계정 권한 부여
# 3. 서비스 계정 JSON 키 다운로드
# 4. GA4_PROPERTY_ID에 속성 ID 입력 (예: 123456789)
# 5. GA4_CREDENTIALS에 서비스 계정 JSON 내용 입력
# 6. .env.local 파일로 복사하고 실제 값으로 교체
# 7. GoatCounter 사이트에서 API 토큰 생성 후 GC_API_TOKEN에 입력
